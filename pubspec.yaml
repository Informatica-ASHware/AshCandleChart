# path: KChart2/pubspec.yaml
# Workspace ROOT. Pub Workspaces (Dart 3.6+) + Melos 7.x.
# Riverpod 3.x + freezed 2.x estable + última versión de codegen.

name: kchart_workspace
description: Workspace root for KChart V2 packages.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

# === Pub Workspaces ===
workspace:
  - kchart_core
  - kchart_flutter
  - kchart_riverpod

dev_dependencies:
  melos: ^7.0.0
